{
  "version": 2,
  "name": "parcel-bootstrap",
  "public": true,
  "alias": ["parcel-bootstrap.now.sh"],
  "builds": [
    {"src": "package.json", "use": "@now/static-build", "config": { "distDir": "dist" }}
  ],
  "routes": [
    {"src": "^/(.*)", "dest": "/index.html"}
  ]
}
